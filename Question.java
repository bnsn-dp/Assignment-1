public interface Question {
	/*
	 * 	Configure the question types and candidate answers. It should support 2 types of
	 * 	questions: 
	 * 		1) multiple-choice question (more than one choice can be chosen including one choice); 
	 * 		2) single choice question (only one choice can be chosen). For each type,
	 
	 * 		you can configure the candidate answers, such as “A”, “B”, “C”, “D”, or “1. Right”, “2. Wrong”, etc.
	 */

}

